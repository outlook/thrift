name: Build
on: [push]
jobs:
  Build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3

      - name: Install dependencies
        run: brew install autoconf automake bison pkg-config boost@1.76

      - name: Boostrap
        run: |
          export PATH="/usr/local/opt/bison/bin:$PATH"
          ./bootstrap.sh

      - name: Configure
        run: |
          export PATH="/usr/local/opt/bison/bin:$PATH"
          export LDFLAGS="-L/usr/local/opt/boost@1.76/lib"
          export CPPFLAGS="-I/usr/local/opt/boost@1.76/include"
          ./configure --with-boost=/usr/local/opt/boost@1.76

      - name: Make
        run: |
          export PATH="/usr/local/opt/bison/bin:$PATH"
          export LDFLAGS="-L/usr/local/opt/boost@1.76/lib"
          export CPPFLAGS="-I/usr/local/opt/boost@1.76/include"
          make

      - uses: actions/upload-artifact@v3
        with:
          name: thrift
          path: ./thrift
